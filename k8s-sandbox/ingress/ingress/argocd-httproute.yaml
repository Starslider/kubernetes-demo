apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd-backend
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: ingress-gateway
      namespace: ingress
  hostnames:
    - "argocd.dhlabs.org"
  rules:
    - backendRefs:
        - kind: Service
          name: argocd-server
          port: 80
          namespace: argocd